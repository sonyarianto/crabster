services:
  crabster:
    build: .
    ports:
      - "8000:8000"
      - "8001:8001"
    volumes:
      - crabster_data:/data
    environment:
      - RUST_LOG=crabster=info
    restart: unless-stopped

volumes:
  crabster_data:
